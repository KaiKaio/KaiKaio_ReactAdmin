name: Build and Push Docker Image

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}

    - name: Build and push Docker image
      uses: docker/build-push-action@v6
      with:
        context: .
        push: true
        tags: ${{ secrets.DOCKER_USERNAME }}/kaikaio-react-admin:latest

  # Optional: Deploy to server via SSH
  # deploy:
  #   needs: build-and-push
  #   runs-on: ubuntu-latest
  #   steps:
  #   - name: Deploy to Server
  #     uses: appleboy/ssh-action@v0.1.10
  #     with:
  #       host: ${{ secrets.SERVER_HOST }}
  #       username: ${{ secrets.SERVER_USER }}
  #       key: ${{ secrets.SSH_PRIVATE_KEY }}
  #       script: |
  #         docker pull ${{ secrets.DOCKER_USERNAME }}/kaikaio-react-admin:latest
  #         docker stop kaikaio-react-admin || true
  #         docker rm kaikaio-react-admin || true
  #         docker run -d --name kaikaio-react-admin -p 80:80 ${{ secrets.DOCKER_USERNAME }}/kaikaio-react-admin:latest
